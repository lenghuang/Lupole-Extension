/* let price = document.getElementById("priceblock_ourprice");

let title = document.getElementById("productTitle");

const productInfo = {
  price: price.innerHTML,
  title: title.innerHTML
};

console.log("Reading in " + productInfo.price);

//export { productInfo };
*/
